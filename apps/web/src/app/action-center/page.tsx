"use client";
import {useState} from "react";import {GovernedActionSurface} from "../../components/governed-action-surface";import "./actions.css";
export default function ActionCenterPage(){const[surface,setSurface]=useState<"modal"|"drawer"|null>(null);return <main id="main-content" className="shell"><header><p className="eyebrow">Controlled interactions</p><h1>Action center</h1><p className="muted">Validated forms replace browser prompts and alerts across support workflows.</p></header><div className="action-card-grid"><article className="panel"><span className="status-pill">Approval</span><h2>Review remediation decision</h2><p>Confirm an exact fingerprinted plan with a typed approval or rejection reason.</p><button type="button" onClick={()=>setSurface("drawer")}>Open action drawer</button></article><article className="panel"><span className="status-pill">Confirmation</span><h2>Confirm employee outcome</h2><p>Use a focused modal for business-function verification and safe cancellation.</p><button type="button" onClick={()=>setSurface("modal")}>Open confirmation modal</button></article><article className="panel"><span className="status-pill">Safety</span><h2>Submission controls</h2><ul><li>Tenant, role and incident scope</li><li>Exact action fingerprint</li><li>Duplicate submission protection</li><li>Secret-content rejection</li></ul></article></div>{surface&&<GovernedActionSurface surface={surface} title={surface==="drawer"?"Remediation decision":"Employee outcome confirmation"} onClose={()=>setSurface(null)}/>}</main>}
