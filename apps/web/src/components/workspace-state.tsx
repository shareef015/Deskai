type StateProps={title:string;description:string;actionLabel?:string;onAction?:()=>void};
export function LoadingState({title="Loading workspace",description="Retrieving the latest authorized data."}:Partial<StateProps>){return <section className="workspace-state" role="status" aria-live="polite" aria-busy="true"><span className="spinner" aria-hidden="true"/><h2>{title}</h2><p>{description}</p></section>}
export function ErrorState({title="Workspace unavailable",description="The view could not be loaded.",actionLabel="Try again",onAction}:StateProps){return <section className="workspace-state error-panel" role="alert"><span aria-hidden="true">!</span><h2>{title}</h2><p>{description}</p>{onAction&&<button type="button" onClick={onAction}>{actionLabel}</button>}</section>}
export function EmptyWorkspaceState({title,description,actionLabel,onAction}:StateProps){return <section className="workspace-state" role="status" aria-live="polite"><span aria-hidden="true">◇</span><h2>{title}</h2><p>{description}</p>{onAction&&actionLabel&&<button type="button" onClick={onAction}>{actionLabel}</button>}</section>}
