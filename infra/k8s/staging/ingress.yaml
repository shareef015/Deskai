apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deskpilot-staging
  namespace: deskpilot-staging
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
spec:
  tls:
    - hosts: ["deskpilot-staging.example.invalid"]
      secretName: deskpilot-staging-tls
  rules:
    - host: deskpilot-staging.example.invalid
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: deskpilot-api
                port:
                  name: http
          - path: /
            pathType: Prefix
            backend:
              service:
                name: deskpilot-frontend
                port:
                  name: http
