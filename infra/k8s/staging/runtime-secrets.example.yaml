# Example only. Never commit real secret values.
# Provision `deskpilot-runtime-secrets` from your KMS/Vault/External Secrets platform.
apiVersion: v1
kind: Secret
metadata:
  name: deskpilot-runtime-secrets
  namespace: deskpilot-staging
type: Opaque
stringData:
  OIDC_ISSUER: "REPLACE_VIA_EXTERNAL_SECRET_PROVIDER"
  OIDC_CLIENT_ID: "REPLACE_VIA_EXTERNAL_SECRET_PROVIDER"
  OIDC_CLIENT_SECRET: "REPLACE_VIA_EXTERNAL_SECRET_PROVIDER"
  DATABASE_URL: "REPLACE_VIA_EXTERNAL_SECRET_PROVIDER"
  REDIS_URL: "REPLACE_VIA_EXTERNAL_SECRET_PROVIDER"
  VECTOR_URL: "REPLACE_VIA_EXTERNAL_SECRET_PROVIDER"
  MODEL_PROVIDER_API_KEY: "REPLACE_VIA_EXTERNAL_SECRET_PROVIDER"
  LANGSMITH_API_KEY: "REPLACE_VIA_EXTERNAL_SECRET_PROVIDER"
  OTEL_EXPORTER_OTLP_ENDPOINT: "REPLACE_VIA_EXTERNAL_SECRET_PROVIDER"
