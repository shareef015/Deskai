[project]
name = "deskpilot-core"
version = "0.1.0"
description = "Runtime-neutral DeskPilot primitives"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
