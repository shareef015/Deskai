[project]
name = "deskpilot-schemas"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.11,<3"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
