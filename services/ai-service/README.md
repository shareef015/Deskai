# AI service

LangGraph orchestration, policy gates, model routing and grounded response assembly.

The typed graph state uses deterministic bounded reducers for messages, evidence,
safe errors and retries, with immutable tenant/incident scope and explicit
consent, approval, budget and terminal-state validation.
